//Array- a data structure to represent a list of items
//tecnicamente un array es un objeto

let selectedColors = ['red', 'blue'];
selectedColors[2] = 'green'
selectedColors[3] = '3'

console.log(selectedColors.filter(pan => pan.includes('n')))
